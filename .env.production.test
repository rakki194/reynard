# Test Environment for CPU-Only Production Testing
# JWT Configuration
JWT_SECRET_KEY=test-super-secure-secret-key-for-production-testing-only
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# Database Configuration
POSTGRES_DB=reynard_test
POSTGRES_USER=reynard_test_user
POSTGRES_PASSWORD=test-secure-database-password-123

# Redis Configuration
REDIS_PASSWORD=test-secure-redis-password-456

# Environment
ENVIRONMENT=production
DEBUG=false

# Domain (for testing)
DOMAIN=localhost
SSL_EMAIL=test@localhost
