import { World } from "../../types";
/**
 * Player input system - handles player movement based on input.
 */
export declare function playerInputSystem(world: World): void;
/**
 * Shooting system - creates bullets when player shoots.
 */
export declare function shootingSystem(world: World): void;
