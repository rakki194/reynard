import { World } from "../../types";
/**
 * Rendering system - prepares entities for rendering.
 */
export declare function renderingSystem(world: World): void;
/**
 * Game state system - manages overall game state.
 */
export declare function gameStateSystem(world: World): void;
