/**
 * 🦊 Test Setup
 * 
 * Setup file for animation package tests
 */

import { beforeAll, afterAll } from 'vitest';

beforeAll(() => {
  // Setup test environment
  console.log('🦊 Setting up animation package tests');
});

afterAll(() => {
  // Cleanup test environment
  console.log('🦊 Cleaning up animation package tests');
});
