name: Valid Test
on: push
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Test with fixable issues
        run: |
          if [ "$VAR" ]; then
            echo "Variable is quoted"
          fi
          echo "Hello World"
          ls -la
