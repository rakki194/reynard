name: Test Fixable Workflow
on: push
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run tests with fixable issues
        run: |
          if [ $VAR ]; then
            echo "Variable not quoted"
          fi
          echo "Hello World"
          ls -la

      - name: Run clean script
        run: |
          echo "This script is clean"
          echo "No issues here"
