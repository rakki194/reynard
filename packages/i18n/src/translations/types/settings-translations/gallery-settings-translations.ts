/**
 * Gallery and view settings translation type definitions for the Reynard i18n system.
 *
 * This module contains gallery view mode, sorting, and display settings
 * translation interfaces.
 */

// Gallery and view settings translations
export interface GallerySettingsTranslations {
  viewMode: string;
  gridView: string;
  listView: string;
  sortBy: string;
  sortByName: string;
  sortByBasename: string;
  sortByDate: string;
  sortByDateOldest: string;
  sortBySize: string;
  sortBySizeSmallest: string;
}
