/**
 * Line Break Processor for handling line breaks and whitespace
 */
import { InlineProcessor } from "./InlineProcessor";
export declare class LineBreakProcessor extends InlineProcessor {
    process(text: string): string;
}
